{
  "name": "@openbulls/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts",
    "./client": "./src/client.ts",
    "./permissions": "./src/permissions.ts"
  },
  "scripts": {
    "build": "echo 'auth is consumed via source'",
    "dev": "echo 'auth is consumed via source'",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@openbulls/config": "workspace:*",
    "@openbulls/db": "workspace:*",
    "better-auth": "^1.1.10",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
